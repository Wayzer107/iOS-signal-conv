Task 1: Bootstrap Desktop Workspace and Test Harness

Actions performed:
- Created desktop-updater workspace with minimal TypeScript + vite config and tauri placeholders.
- Implemented src/App.tsx exporting runAppSmoke() -> 'ready'.
- Added vitest smoke test at tests/smoke/app-smoke.test.ts per brief.
- Committed changes: chore: bootstrap desktop updater workspace

Notes and verification:
- Files created:
  - desktop-updater/package.json
  - desktop-updater/tsconfig.json
  - desktop-updater/vite.config.ts
  - desktop-updater/src/main.tsx
  - desktop-updater/src/App.tsx
  - desktop-updater/src-tauri/Cargo.toml
  - desktop-updater/src-tauri/src/main.rs
  - desktop-updater/src-tauri/tauri.conf.json
  - desktop-updater/tests/smoke/app-smoke.test.ts

- Unable to run pnpm/npm tests in this environment: Node.js and pnpm are not available in the current runtime, so I could not execute `pnpm test` to verify the test run. The implementation is minimal and should pass when run in a Node environment with dependencies installed.

How to run locally:
1. cd desktop-updater
2. pnpm install (or npm install)
3. pnpm test tests/smoke/app-smoke.test.ts

If you want, I can re-run tests in CI or a Node-capable environment.

Report generated by Copilot CLI automation.

Fixes applied (Task 1 scope):
- Added desktop-updater/e2e.config.ts so the existing script "test:e2e" points to a present Vitest config.
- Added desktop-updater/index.html so `pnpm dev` (Vite) can start the app.

Commands run and outputs (important lines):
- git add desktop-updater/e2e.config.ts desktop-updater/index.html && git commit -m "fix(task-1): provide e2e vitest config and index.html for vite dev"
  -> [wayzer107-using-superpowers-skill 7eae2bc] fix(task-1): provide e2e vitest config and index.html for vite dev
     2 files changed, 21 insertions(+)
- cd desktop-updater && pnpm test:e2e
  -> /bin/bash: pnpm: command not found

Test re-run notes:
- I attempted to re-run the tests that cover the changed files with `cd desktop-updater && pnpm test:e2e` but pnpm is not available in this runtime, so the command failed as shown above. To verify locally: run `pnpm install` then `pnpm test` or `pnpm test:e2e`.

Reviewer note on parity / E2E coverage:
- The reviewer flagged branch-level parity/E2E coverage. This Task 1 change only fixes config and dev bootstrap issues. Branch-level acceptance and cross-branch E2E tests are planned in later tasks (Task 7 and related) and are not implemented here.


